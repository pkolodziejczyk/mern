<%- include('header', {title: "Product"}); %>
   <%= product.title %>
   <%= product.price %>
   <%= product.description %>
   <% if (product.categories) { %>
      <%= product.categories.name %>
   <% } %>
   <br>

<%- include('footer'); %>
